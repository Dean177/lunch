machine:
  node:
    version: 5.1.0

dependencies:
  override:
    - npm set progress=false
    - npm install

test:
  override:
    - npm run build:client
    - npm run build:server
    - npm run test:unit
    - npm run test:lint
