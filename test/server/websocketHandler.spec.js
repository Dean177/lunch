import { expect } from 'chai';
import { OptionChoices } from '../../src/shared/constants/actionTypes';
import configureWebsocketHandler from '../../src/server/websocketHandler/index';


describe('websocketHandler', () => {
  it('Should send the current lunch options and choices once a user connects');

  it('Should broadcast to all clients when a user changes their image');
});
